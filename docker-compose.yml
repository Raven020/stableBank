services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: stablebank
      POSTGRES_PASSWORD: stablebank
      POSTGRES_DB: stablebank
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
